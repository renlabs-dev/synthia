# OpenAI, goes for both miner and validator, although validator utilizes only OPENAI_API_KEY from this file
OPENAI_API_KEY=my-key
# Recommend using model gpt-4-turbo-preview / gpt-4-0125-preview
OPENAI_MODEL=gpt-4-turbo-preview
# keep rather above 500 >
OPENAI_MAX_TOKENS=1000
OPENAI_TEMPERATURE=0.1
